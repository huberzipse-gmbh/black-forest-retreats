/**
 * Arabische Strings — Phase 1A: Platzhalter, inhaltlich noch Deutsch.
 * Struktur ist über `Strings` fixiert; in einer späteren Phase Werte übersetzen.
 */
import { de } from './de';
import type { Strings } from './de';

export const ar: Strings = de;
