/**
 * Englische Strings — Phase 1A: Platzhalter, inhaltlich noch Deutsch.
 * Struktur ist über `Strings` fixiert; in einer späteren Phase Werte übersetzen.
 * (Vorerst spiegeln wir den deutschen Inhalt, damit alles kompiliert und der
 *  Sprach-Mechanismus testbar ist, ohne dass „leere" Sprachen entstehen.)
 */
import { de } from './de';
import type { Strings } from './de';

export const en: Strings = de;
